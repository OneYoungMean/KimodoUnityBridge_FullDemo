name: sync-bridge-package

on:
  workflow_dispatch:
    inputs:
      bridge_ref:
        description: Ref/branch/tag of OneYoungMean/KimodoUnityBridge
        required: false
        default: main
      base_branch:
        description: Target branch for the pull request in KimodoUnityBridge_FullDemo
        required: false
        default: main

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: sync-bridge-package
  cancel-in-progress: false

jobs:
  sync:
    runs-on: windows-latest

    steps:
      - name: Checkout KimodoUnityBridge_FullDemo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout KimodoUnityBridge
        uses: actions/checkout@v4
        with:
          repository: OneYoungMean/KimodoUnityBridge
          ref: ${{ github.event.inputs.bridge_ref || 'main' }}
          path: __bridge_source

      - name: Mirror package into Packages/KimodoUnityBridge
        shell: pwsh
        run: |
          $source = Join-Path $env:GITHUB_WORKSPACE "__bridge_source"
          $target = Join-Path $env:GITHUB_WORKSPACE "Packages\KimodoUnityBridge"

          New-Item -ItemType Directory -Force -Path $target | Out-Null
          robocopy $source $target /MIR /XD .git .github | Out-Null
          $robocopyExitCode = $LASTEXITCODE
          if ($robocopyExitCode -ge 8) {
            throw "robocopy failed with exit code $robocopyExitCode"
          }
          $global:LASTEXITCODE = 0

      - name: Detect package changes
        id: package_changes
        shell: pwsh
        run: |
          git add Packages/KimodoUnityBridge
          if (git diff --cached --quiet) {
            "changed=false" >> $env:GITHUB_OUTPUT
          }
          else {
            "changed=true" >> $env:GITHUB_OUTPUT
          }

      - name: Create sync pull request
        if: steps.package_changes.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: chore: sync KimodoUnityBridge package from ${{ github.event.inputs.bridge_ref || 'main' }}
          branch: bot/sync-bridge-package-${{ github.event.inputs.base_branch || 'main' }}
          delete-branch: true
          title: chore: sync KimodoUnityBridge package into ${{ github.event.inputs.base_branch || 'main' }}
          body: |
            This PR was created automatically by `sync-bridge-package`.

            Source ref: `${{ github.event.inputs.bridge_ref || 'main' }}`
            Base branch: `${{ github.event.inputs.base_branch || 'main' }}`
            Target path: `Packages/KimodoUnityBridge`
            Workflow run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          base: ${{ github.event.inputs.base_branch || 'main' }}
          add-paths: |
            Packages/KimodoUnityBridge

      - name: Summary
        shell: pwsh
        run: |
          @(
            "Synced bridge ref: ${{ github.event.inputs.bridge_ref || 'main' }}"
            "Pull request base branch: ${{ github.event.inputs.base_branch || 'main' }}"
            "Target path: Packages/KimodoUnityBridge"
            "Package changes detected: ${{ steps.package_changes.outputs.changed || 'false' }}"
          ) | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Encoding utf8 -Append
